## tc-P2-功能测试：不同语种切换-IPPool相关页面文案正确

### 前置条件 (pc)
- 存在 IPPool 数据

### 测试步骤 & 预期结果
1. 切换系统语言为中文 → IPPool 列表/创建/编辑/详情页文案显示中文，正确
2. 切换系统语言为 English → IPPool 列表/创建/编辑/详情页文案显示英文，正确

## tc-P2-功能测试：不同语种切换-网络配置页面文案正确

### 前置条件 (pc)
- 企业空间下项目已绑定 IPPool

### 测试步骤 & 预期结果
1. 切换系统语言为中文 → 网络配置页文案显示中文，正确
2. 切换系统语言为 English → 网络配置页文案显示英文，正确

## tc-P2-功能测试：不同语种切换-工作负载容器组页面文案正确

### 前置条件 (pc)
- 存在已分配 IP 的容器组

### 测试步骤 & 预期结果
1. 切换系统语言为中文 → 容器组列表/详情页文案显示中文，IP地址字段标签正确
2. 切换系统语言为 English → 容器组列表/详情页文案显示英文，IP地址字段标签正确

## tc-P2-功能测试：不同角色权限-IPPool 权限正确

### 前置条件 (pc)
- 准备不同角色的测试用户

### 测试步骤 & 预期结果
1. 使用 cluster-admin 角色登录 → 可查看/创建/编辑/删除 IPPool
2. 使用企业空间普通用户角色登录 → 仅可查看 IPPool 列表和详情，无法创建/编辑/删除
3. 无权限用户登录 → 页面提示无权限或不可见
4. 企业空间普通用户角色 → 可查看网络配置页面，不可编辑/删除

## tc-P3-功能测试：接口权限校验-IPPool API

### 前置条件 (pc)
- 获取无权限用户的 token

### 测试步骤 & 预期结果
1. 使用无权限用户的 token，调用 IPPool 查询 API → 返回 403 或适当错误码
2. 使用无权限用户的 token，调用 IPPool 创建 API → 返回 403 或适当错误码
3. 使用无权限用户的 token，调用 IPPool 编辑 API → 返回 403 或适当错误码
4. 使用无权限用户的 token，调用 IPPool 删除 API → 返回 403 或适当错误码
5. 使用无权限用户的 token，调用网络配置查询 API → 返回 403 或适当错误码
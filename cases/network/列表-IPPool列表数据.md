## tc-P1-功能测试：IPPool列表数据及字段显示正确

### 前置条件 (pc)
- 存在 IPv4/IPv6/DualStack 类型的 IPPool 数据

### 测试步骤 & 预期结果
1. 进入【集群】-【容器组IP池】 → 显示 IPPool 列表，列表数据正确
2. 查看列表字段（名称、状态、类型、网段、已用IP地址、IP池块大小、Overlay模式、NAT outgoing） → 各字段显示正确
3. 查看名称列，存在别名和描述的 IPPool → 显示为 `alias-test(ippool-ceda-1)` 格式，别名在上，下方显示描述
4. 查看类型列，不同类型（IPv4/IPv6/DualStack）的 IPPool → 类型图标和文字区分显示正确
5. 查看状态列 → 状态显示正确：已启用（绿色）/已禁用（灰色）

## tc-P1-功能测试：IPPool列表查询功能正确

### 前置条件 (pc)
- 存在多条 IPPool 数据，覆盖不同名称、状态、类型

### 测试步骤 & 预期结果
1. 进入【集群】-【容器组IP池】 → 显示 IPPool 列表
2. 按名称模糊搜索，输入关键词 → 列表实时筛选，显示匹配名称的 IPPool
3. 按状态筛选，选择：已启用 → 列表仅显示已启用的 IPPool
4. 按状态筛选，选择：已禁用 → 列表仅显示已禁用的 IPPool
5. 按类型筛选，选择：IPv4 → 列表仅显示 IPv4 类型的 IPPool
6. 按类型筛选，选择：IPv6 → 列表仅显示 IPv6 类型的 IPPool
7. 按类型筛选，选择：DualStack → 列表仅显示 DualStack 类型的 IPPool
8. 输入不存在的名称 → 显示空状态提示
9. 组合查询（名称 + 状态 + 类型） → 结果正确交集

## tc-P3-功能测试：IPPool列表分页及空数据

### 前置条件 (pc)
- IPPool 数据超过一页 / 无数据

### 测试步骤 & 预期结果
1. 进入【集群】-【容器组IP池】，数据超过一页 → 分页控件正常显示
2. 点击下一页 → 数据正确切换至下一页
3. 携带查询条件翻页 → 查询条件不丢失
4. 快速跳转至指定页码 → 跳转正确
5. 无 IPPool 数据时 → 显示空状态提示

## tc-P3-功能测试：IPPool列表排序功能正确

### 前置条件 (pc)
- 存在多条 IPPool 数据

### 测试步骤 & 预期结果
1. 进入【集群】-【容器组IP池】 → 显示列表
2. 点击可排序字段（如名称、类型、网段等）的列标题 → 列表按该字段升序排列
3. 再次点击同一列标题 → 列表按该字段降序排列

## tc-P3-功能测试：IPPool列表名称列显示-别名描述为空

### 前置条件 (pc)
- 存在别名和描述均为空的 IPPool

### 测试步骤 & 预期结果
1. 进入【集群】-【容器组IP池】 → 显示列表
2. 查看别名和描述为空的 IPPool 名称列 → 仅显示 IPPool 名称，无别名/描述行

## tc-P3-功能测试：IPPool列表筛选条件切换重置

### 前置条件 (pc)
- 存在多条 IPPool 数据

### 测试步骤 & 预期结果
1. 进入【集群】-【容器组IP池】，选择类型筛选为 IPv4 → 列表仅显示 IPv4
2. 切换类型筛选为 IPv6 → 列表刷新为 IPv6 数据
3. 切换类型筛选为"全部类型" → 列表显示所有类型

## tc-P2-功能测试：IPPool列表多语种切换

### 前置条件 (pc)
- 存在 IPPool 数据

### 测试步骤 & 预期结果
1. 切换系统语言为中文 → 列表字段文案显示中文（名称、状态、类型、网段、已用IP地址等）
2. 切换系统语言为 English → 列表字段文案显示英文（Name、Status、Type、CIDR等）
## tc-P1-功能测试：IPPool 详情-基础信息数据正确

### 前置条件 (pc)
- 存在 IPv4/IPv6/DualStack 类型的 IPPool

### 测试步骤 & 预期结果
1. 进入【集群】-【容器组IP池】，点击 IPPool 名称进入详情 → 进入详情页
2. 查看基础信息字段（IP池名称、状态、类型、网络、已用IP地址、IP池块大小、NAT outgoing、Overlay模式、掩码、起始IP地址、结束IP地址、创建时间、创建者） → 各字段数据正确
3. 分别查看 IPv4/IPv6/DualStack 类型的 IPPool 详情 → 网络字段显示对应类型网段，起始/结束IP地址正确

## tc-P2-功能测试：IPPool 详情-项目 Tab 数据正确

### 前置条件 (pc)
- 存在已关联项目的 IPPool

### 测试步骤 & 预期结果
1. 进入 IPPool 详情页 → 基础信息显示正确
2. 切换至【项目】Tab → 显示已关联该 IPPool 的项目列表，数据正确

## tc-P2-功能测试：IPPool 详情-容器组 Tab 数据正确

### 前置条件 (pc)
- 存在已关联 IPPool 的容器组

### 测试步骤 & 预期结果
1. 进入 IPPool 详情页，切换至【容器组】Tab → 显示容器组列表
2. 查看字段（容器组名称、节点、容器组IP地址、CPU监控、内存监控） → 各字段显示正确
3. 容器组IP地址列 → 根据 IPPool 类型显示 IPv4/IPv6/双栈 地址格式正确

## tc-P3-功能测试：IPPool 详情-各 Tab 空数据处理

### 前置条件 (pc)
- 存在 IPPool，但未关联任何项目/容器组

### 测试步骤 & 预期结果
1. 进入 IPPool 详情页 → 基础信息显示正确
2. 切换至【项目】Tab，无关联项目 → 显示空状态提示
3. 切换至【容器组】Tab，无关联容器组 → 显示空状态提示

## tc-P3-功能测试：IPPool 详情-项目/容器组 Tab 分页

### 前置条件 (pc)
- 存在 IPPool，关联超过一页的项目和容器组

### 测试步骤 & 预期结果
1. 进入 IPPool 详情页，切换至【项目】Tab，数据超过一页 → 分页控件正常，翻页正确
2. 切换至【容器组】Tab，数据超过一页 → 分页控件正常，翻页正确
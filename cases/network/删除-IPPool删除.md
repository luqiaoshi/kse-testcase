## tc-P1-功能测试：列表页删除 IPPool，成功

### 前置条件 (pc)
- 存在任意类型的 IPPool

### 测试步骤 & 预期结果
1. 进入【集群】-【容器组IP池】 → 显示 IPPool 列表
2. 操作列：删除 → 弹出二次确认
3. 输入匹配信息，点击：确认 → 删除成功，列表不再显示已删除的 IPPool

## tc-P3-功能测试：删除 IPPool-取消操作

### 前置条件 (pc)
- 存在任意类型的 IPPool

### 测试步骤 & 预期结果
1. 进入【集群】-【容器组IP池】，操作列：删除 → 弹出二次确认
2. 点击：取消 → 关闭弹窗，IPPool 未删除，列表数据不变

## tc-P3-功能测试：删除 IPPool-名称不匹配

### 前置条件 (pc)
- 存在任意类型的 IPPool

### 测试步骤 & 预期结果
1. 进入【集群】-【容器组IP池】，操作列：删除 → 弹出二次确认
2. 输入不匹配的名称 → 确认按钮置灰或点击后提示名称不匹配
3. 输入正确的匹配名称 → 可点击确认，删除成功

## tc-P2-功能测试：删除有关联容器组的 IPPool

### 前置条件 (pc)
- 存在已关联容器组的 IPPool

### 测试步骤 & 预期结果
1. 进入【集群】-【容器组IP池】，操作列：删除 → 弹出二次确认
2. 二次确认中提示有关联容器组数量或信息 → 提示信息正确
3. 输入匹配名称，点击：确认 → 删除成功或被拦截（按需求决定是否允许删除）